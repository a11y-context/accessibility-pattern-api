# accessibility-pattern-api — Claude operating instructions

## What this repo is

A11y Context's **corpus** and **documentation site**. Two things live here, intentionally co-located:

1. **The corpus** — WCAG-derived accessibility patterns and global rules, structured for selective retrieval by AI coding agents. Source of truth lives under `patterns/<stack>/`.
2. **The documentation site** — Docusaurus 3.7 deployed to Vercel at https://a11y-context-project.vercel.app, sourced directly from `patterns/` plus hand-authored content in `website/docs/`.

What does NOT live here:

- **Installable skill artifacts** (SKILL.md files, ZIPs) live in [`a11y-context/a11y-context-skills`](https://github.com/a11y-context/a11y-context-skills). This repo's build pipeline pulls from that repo to generate per-skill ZIP downloads.
- **Experimental variants** from the 9-variant evaluation are in [`a11y-context-archive`](https://github.com/a11y-context-archive) as read-only historical artifacts.

## Repository layout

```
patterns/                            ← corpus (source of truth)
  web/react/
    patterns.json                    ← machine index (catalog)
    components/                      ← one .md per component pattern
    global_rules.md                  ← cross-cutting rules (Foundations)
    component-gallery.md             ← generated from patterns.json (prebuild)
    foundations.md
    release-notes.md
  android/                           ← stub (Coming soon)
  ios/                               ← stub (Coming soon)

website/                             ← Docusaurus site (sourced from patterns/)
  docs/
    getting-started/                 ← Getting Started + AI-coding-agents branch + QA-testing branch
    troubleshooting/
  src/pages/index.js                 ← landing page
  src/css/custom.css                 ← theme
  static/                            ← static assets served at site root
    downloads/                       ← generated ZIPs (prebuild; gitignored)
  scripts/
    generate-component-gallery.js    ← prebuild: regenerates component-gallery.md
    generate-skill-downloads.js      ← prebuild: clones skills repo, builds ZIPs
  .skills-cache/                     ← clone target for skills monorepo (gitignored)
  docusaurus.config.js
  sidebars-*.js

LICENSE                              ← Apache 2.0
CONTRIBUTING.md                      ← contributor terms + scope rules
README.md
```

## Where to write what

| Goal | File location |
|---|---|
| Add a new component pattern | `patterns/<stack>/components/<pattern-id>.md` + add an entry to `patterns/<stack>/patterns.json` |
| Revise an existing pattern | edit `patterns/<stack>/components/<pattern-id>.md` directly; bump `catalog_revision` in `patterns.json` |
| Update a global / foundational rule | `patterns/<stack>/global_rules.md` |
| Site content (Getting Started, troubleshooting, etc.) | `website/docs/<section>/...` |
| Landing-page copy | `website/src/pages/index.js` |
| Site theme | `website/src/css/custom.css` |
| Build pipeline change | `website/scripts/` + wire into `website/package.json` `prebuild` |

**Do NOT hand-edit:**
- `patterns/<stack>/component-gallery.md` between the `gallery:start` / `gallery:end` markers — regenerated by `npm run gen:gallery`
- `website/static/downloads/*.zip` — regenerated by `npm run gen:downloads`

## Build pipeline

`npm run build` (from `website/`) runs Docusaurus build. Before that, the `prebuild` chain runs in order:

1. `generate-component-gallery.js` — reads `patterns.json`, regenerates the catalog table in `component-gallery.md`
2. `generate-skill-downloads.js` — clones [`a11y-context/a11y-context-skills`](https://github.com/a11y-context/a11y-context-skills) into `website/.skills-cache/`, walks `skills/<platform>/<variant>/`, reads each SKILL.md frontmatter to determine the canonical name, generates `static/downloads/<canonical-name>.zip`

Both run automatically on Vercel build. Run manually with `npm run gen:gallery` or `npm run gen:downloads`.

## What never to commit

Listed in `.gitignore`, repeated here for visibility:

- `website/build/` — Docusaurus build output (Vercel builds from source)
- `website/.skills-cache/` — clone of the skills monorepo; re-fetched on every build
- `website/static/downloads/` — generated ZIPs; regenerated by prebuild
- `node_modules/`, build caches, `.DS_Store`

## Pattern format and conventions

Two canonical references, read both before authoring or revising a pattern:

- **`schema/pattern-template.md`** — the format (frontmatter contract, section order, bare template to copy).
- **`schema/style-guide.md`** — the conventions (per-section guidance, prose rules, boilerplate formulas, Customizable typology, Golden Pattern code rules, definition of done, write-like-this examples).

Existing patterns under `patterns/web/react/components/` are good shape references. `button.basic.md`, `dialog.modal.md`, and `accordion.basic.md` cover the typical variations (simple component, focus-managed component, hierarchy-dependent component).

## What goes in the corpus

Patterns and rules derived from WCAG 2.2 AA, WAI-ARIA, the ARIA Authoring Practices Guide (APG), and other public accessibility standards. Golden Patterns use standard HTML, ARIA, and framework-standard primitives.

## Versioning

See `CONTRIBUTING.md` § Versioning for the contributor-facing rules. The vault's `01 - Projects/A11y-Context-Project/context-load.md` § Corpus Edit Protocol carries the behavioral version for Claude — surface a version-bump proposal on every corpus edit using the form "X.Y.Z because <reason>", and check contributor PRs for aligned bumps and release-notes entries.

## Cross-repo context

- **Skills monorepo:** [`a11y-context/a11y-context-skills`](https://github.com/a11y-context/a11y-context-skills) — installable rule/skill artifacts. The Downloads page pulls from here at build time.
- **Archive org:** [`a11y-context-archive`](https://github.com/a11y-context-archive) — 11 read-only repos from the original 9-variant evaluation, kept for historical reproducibility. Two carry supersession banners (`claude-rule-skill-local`, `claude-rule-skill-http`).
- **Vault (private):** John's private project tracker, decisions log, and authoring canon (style guide, workflow, improvement notes) live at `~/01 - Projects/A11y-Context-Project/` and `~/01 - Projects/A11y-Context-Project/authoring/`. Not in this repo by deliberate choice — see [decisions log entry 2026-06-04](https://github.com/a11y-context/...). May migrate to a public `authoring/` directory in this repo if multi-contributor authoring scales up (open question).

## Authoring workflow (when adding a new component pattern)

The canonical workflow currently lives in John's vault at `authoring/authoring-workflow.md` (private). The shape, summarized:

1. **Outline** — author identifies the component, its use cases, and its `Use When` / `Do Not Use When` boundaries
2. **Pattern draft** — write the pattern markdown following `schema/pattern-template.md` and the style guide
3. **Lab demo** — build a minimal interactive demo in the [a11y-pattern-lab](https://github.com/jsweetdude/a11y-pattern-lab) repo for AT verification
4. **AT testing** — author verifies with VoiceOver / NVDA / keyboard; iterates pattern + demo until both pass
5. **Strip to golden** — reduce the lab demo to a minimal Golden Pattern fragment for inclusion in the pattern markdown
6. **Register** — add to `patterns.json`; bump `catalog_revision`; verify the gallery rebuilds; commit

External contributors: follow [`CONTRIBUTING.md`](./CONTRIBUTING.md) and the `schema/pattern-template.md` format. Reference existing patterns for shape.

## License

Apache 2.0 — see [LICENSE](./LICENSE). Contributions are licensed under Apache 2.0 per [`CONTRIBUTING.md`](./CONTRIBUTING.md).
